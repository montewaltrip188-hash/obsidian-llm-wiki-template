# Codex 项目指令

本仓库同时兼容 Claude Code 与 Codex。项目规则主文件是 `CLAUDE.md`。

开始任何工作前：
- 先读取仓库根目录的 `CLAUDE.md`。
- 将其中适用于项目约定、目录结构、知识库流程、禁止事项、验证命令的内容视为本 `AGENTS.md` 的有效规则。
- 遇到 Claude Code 专属工具名、命令写法或路径约定时，按 Codex 当前可用工具等价执行，不要生搬硬套工具名。
- 如果 `CLAUDE.md` 与本文件冲突，以本文件为准。
