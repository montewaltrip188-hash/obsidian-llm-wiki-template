# LLM Wiki Schema

> LLM 维护 wiki 的操作规范。

## 开场规则

- 每次新对话开始时，列出 `log/` 目录下当月子目录，读取最近 3 天的日志文件，了解近期协作上下文。如跨月则同时查看上月子目录。
- 同时读 `wiki/index.md` 了解知识库规模和领域分布（总表），需要某领域详细页面列表时再按需读对应的 `wiki/domains/` 分表。

## 输出规则

### 是否落盘

判断是以下三种内容类型则落盘：

- **wiki/**：ingest 或 query 中沉淀出的可复用知识（实体页、概念页、对比页）—— ingest 按用户明确发起的写入请求维护；query 默认只读，只有按 Auto-Enrichment 展示具体 diff 并获得用户明确确认后才能写回。
- **inbox/**：学习规划、灵感困惑、每日复盘、新闻定时产出、计划任务，按 `news/`、`learning/`、`dailyreview/`、`sparks/`、`tasks/` 子目录分类 —— AI 在学习/新闻/复盘流程中主动维护；`tasks/` 的 AI 写入必须经过用户逐项确认，`sparks/Dailynote/` 主要由工作台插件与用户维护。
- **output/**：是应用知识得到的产出性交付物（skill、标准流程、方案、报告等），按项目子目录组织。
- 以上都不是的（日常对话、改配置、查信息等）→ 直接聊天输出。

### 落盘后的呈现

落盘时聊天窗口只给文件路径和简要说明，不重复正文。

每日复盘是例外的连续流程：复盘文件落盘后，仍须立即进入“候选待办建议”阶段，不得只返回路径就结束。用户确认“落盘”只授权保存复盘，不授权直接写任务；候选任务经用户调整并明确确认后，才能写入工作台。



## 架构概览

```
vault/
├── raw/                  # 原始素材（正文不可修改或删除；已核验来源可补充 frontmatter）
│   ├── assets/           # 下载的图片和附件
│   └── *.md              # 剪藏的文章、论文、笔记
├── wiki/                 # LLM 生成和维护的知识库（记忆/理解）
│   ├── sources/          # 每篇原始素材的摘要页
│   ├── entities/         # 实体页（人物、组织、工具、项目）
│   ├── concepts/         # 概念页（主题、理论、方法论）
│   ├── comparisons/      # 对比分析页
│   ├── domains/          # 领域分表索引（每个领域一个文件）
│   └── index.md          # 总表索引（交叉矩阵 + 领域目录）
├── inbox/                # 知识的理解区（wiki → skills/output 的中间地带）
│   ├── news/             # 新闻定时任务产出
│   ├── learning/         # 学习规划和笔记（课程、书籍）
│   ├── dailyreview/      # 每日复盘（YYYY-MM/YYYY-MM-DD.md）
│   ├── sparks/           # 灵感、困惑、待验证的想法
│   │   └── Dailynote/    # 随手记（工作台插件按日追加，YYYY-MM-DD.md）
│   └── tasks/            # 计划任务（type: plan；任务行含 📅/⏫/✅；AI 写入前必须获得用户明确确认）
├── skills/               # 通用可复用的标准化流程（非项目特定）
│   └── *.md              # 如：提示词写作、竞品分析方法等
├── output/               # 产出性交付物（方案、报告等）
│   ├── 项目A/             # 按项目/目的建子目录组织
│   ├── 项目B/
│   └── index.md          # 产物索引
├── schema/               # 系统规则文件（领域规则、lint 规则、页面模板）
│   ├── domain-rules.md  # 阈值规则、ingest 判断流程、索引维护规范
│   ├── lint-rules.md    # lint 检查项和执行方式
│   └── templates.md     # 页面和日志的格式模板
├── lint/                  # 知识库健康检查报告
│   └── YYYY-MM-DD.md
├── log/                  # 协作日志（按月分目录，每天一个文件）
│   └── YYYY-MM/
│       └── YYYY-MM-DD.md
├── .archive/             # 归档区（已废弃但保留的历史文件，不纳入活跃体系）
├── .snapshots/           # CLAUDE.md 修改前备份
└── CLAUDE.md             # 本文件：Schema 配置
```

## 核心原则

1. **raw/ 正文不可修改删除**：raw/ 中已有文件的正文不得修改或删除，可以读取和创建新文件（如 MinerU 解析结果）；允许基于已核验来源补充 frontmatter 元数据。补充元数据时不得改写正文，必须保留可追溯来源并在当天日志记录变更。
2. **wiki/ 由 LLM 维护**：用户阅读，LLM 编写和更新。
3. **inbox/ 是理解区**：wiki（记忆）到 skills/output（应用）的中间地带，内容流动性强，学透后提炼为 skill 或归档。
4. **skills/ 存放通用可复用流程**：从 wiki 知识中提炼的标准化、流程化操作方法（如提示词写作、竞品分析方法论）。只放通用的、不与特定项目绑定的 skill。
5. **output/ 存放交付物**：是应用知识得到的产出性交付物（skill、标准流程、方案、报告等），按项目子目录组织。项目特定的操作流程/skill（如微信公众号排版规范）放在 `output/` 对应项目目录中，方便使用时直接查看。
6. **知识递增积累**：每次 ingest 不只是添加新页面，还要更新已有页面的交叉引用。
7. **引用溯源**：wiki 和 output 页面中的关键论点必须链接回 `raw/` 或 `wiki/` 中的来源。
8. **单库多领域**：所有领域的知识放在同一个库中，跨领域连接是知识库最有价值的部分。领域区分通过 frontmatter 的 `domain` 字段实现——wiki 页面本身不按领域拆分文件夹（平铺在各类型目录下），`wiki/domains/` 仅作为索引视图，从领域维度组织页面列表以便快速查找。
9. **领域层级**：领域支持树状层级（路径式 `domain: [编程/Python, AI]`），子领域按阈值自动展开。LLM ingest 时必先读 [[wiki/index.md]] 了解当前领域清单，读 [[schema/domain-rules.md]] 了解阈值规则和判断流程。
10. **CLAUDE.md 修改前备份**：任何对 CLAUDE.md 的修改前，先将当前版本复制到 `.snapshots/CLAUDE.md.YYYY-MM-DD.md`（同日多次修改追加 `-HH-MM`）。

## 操作流程

**路由表** — 按用户说法/触发条件对应到流程，新对话先查此表：

| 触发 / 用户怎么说                          | 走哪条流程     | log verb |
| ----------------------------------- | --------- | -------- |
| 「摄入」/「ingest」/「建一页」                 | 1. Ingest → `design-juan-wiki` | `ingest` |
| 「我之前怎么看 X」/「查 X」/ 普通问答              | 2. Query  | `query`  |
| Query 读了 wiki 页且对话产生新认知（自动评估并提议，不自动写入） | 2. Query → Auto-Enrichment | `enrich` |
| 「存下来」/「帮我写一份方案/报告」                  | 3. Output | `output` |
| 「lint」/「健康检查」/「小检查」/「大检查」           | 4. Lint   | `lint`   |
| 「开始今天的复盘」/「把今天的复盘落盘」               | 5. Daily Review | `daily` |
| 复盘后的候选任务 + 用户明确确认                       | 5. Daily Review → 日程转化 | `daily` |
| 手动小改（补 wikilink、改 frontmatter、调索引等） | 无专属流程，直接改 | `update` |
| 其它对话 +「记一下」                         | 5. Daily  | `daily`  |

### 1. Ingest（摄入知识）

> 执行 Ingest 时只调用唯一活跃的 `design-juan-wiki` Skill；旧 `design-shi-wiki` 仅保留为显式兼容入口，不得参与正常路由或提供执行规则。

**第 0 步：读领域规则页**
读 [[wiki/index.md]] 了解当前领域清单（领域目录 + 子领域清单），读 [[schema/domain-rules.md]] 了解阈值规则和 domain 判断流程。按规则选 domain，索引维护也按该页规范执行。

**写入原则：**
- **追本溯源**：记录"为什么"而不只是"是什么"，追溯到底层原理而非转述别人的结论。如：不只是记"Karpathy 说 RAG 不积累知识"，而是记"RAG 每次查询重新检索，没有编译步骤，所以知识不积累"。
- **不重复创建**：创建 concept/entity 前先检查知识库是否已有对应页面，已有则更新，不新建
- **矛盾标记**：发现不同来源的矛盾观点时，明确标记矛盾（用 contradicts 关系标签），不悄悄选一个。两个都记，标注各自的前提条件。
- **上下文保留**：记录结论时保留前提条件/适用范围。"数据量 < 1000 条时 LIKE 够用"而非"LIKE 够用"。
- **时效性标注**：含版本号/定价/时效性词汇（"最新""目前"）时标注日期。如"截至 2026-07，bge-m3 是 C-MTEB 前列"。
- **信源置信度**：创建 entity 页面时，联网搜索了解这个人/组织，给出初始 confidence 值（0.0-1.0），告知用户理由并询问是否调整。单片入库的素材不需要标 confidence。同一信源第二次出现时再提醒建 entity 页。详见 [[schema/templates.md]] 的 Entity 模板

**写入后自查**：wiki 页写完后回到素材，确认关键内容未被压扁或遗漏。

- 有章节结构的素材：对照大标题，查遗漏、查展开不足
- 无结构素材：先脑列「知识单元」（新概念/案例/数据点/方法），再逐条对照
- 对照完写入当天 log：`素材 N 个章节/知识单元 → 覆盖 N 个，展开不足/遗漏：xxx（已补）`

> 教训（2026-07-23）：Harness 101 第二篇 ingest，解剖/长程/收敛/落地四章在概念页被压成表格。对照漏做。

### 2. Query（查询）

1. 调用 `wiki-hybrid-search` skill 的 `search` 命令混合检索（自动排除 sources/）。
2. 有相关页面 → 读页面全文 → 综合回答并引用 `[[wiki/xxx/页面名.md]]`。
3. 无相关页面→ 直接回答。
4. 回答时参考 source 对应 entity 的 confidence，区分"权威来源"和"存疑来源"。
5. 回答中沉淀出可复用知识时，进入 Auto-Enrichment 判断；先回答并展示具体 diff，只有用户明确确认后才能写回 `wiki/`。未确认或用户要求只读时不得修改知识库、索引或日志。

#### Auto-Enrichment（顺手反哺）

Query 的认知副产品经用户确认后回收到 wiki，让知识持续 compound。先回答用户问题，回答完毕后再判断是否提议反哺；自动评估不等于自动写入。

**触发条件（命中任一即提议反哺）：**
- 对话产生了对该页已有论断的**修正、补充、反例或边界条件**
- 对话中提到了该页「关联页面」段**未链接的实体/概念**
- 用户对该页观点表达了**不同解读或新视角**

**不触发（防止噪音）：**
- 对话只是**复述了该页已有内容**——没有新信息不写回
- 单次 query 涉及 ≥3 个 wiki 页时，只反哺**最相关的 1-2 个**，不散弹扫全库
- 用户明确说"别改 wiki"时跳过

**执行流程：**
1. 提出反哺提议：必须列出可直接审核的候选 diff，包括目标文件、命中的触发条件、原文／插入锚点和拟写入的新文本。只说“建议补一段”或概述修改方向不算具体 diff
2. 用户确认后执行修改，更新该页 `updated` 字段
3. 若修改涉及 domain 字段变更（增/删/改）或页面新增了跨域关联，同步更新 `wiki/index.md` 交叉矩阵和受影响的 `wiki/domains/` 分表
4. 在 `log/` 当天日志追加 `enrich` 类型记录

**与 ingest/update 的区别：**
- `ingest`：用户主动发起，专门 session 建/扩 wiki 页
- `update`：手动小改（调 frontmatter、补 wikilink、改索引等）
- `enrich`：query 副作用驱动的写回——本规则触发

### 3. Output（保存交付物）

当产生产出性交付物（skill、标准流程、方案、报告等）时，直接落盘：

1. 将内容写入 `output/项目目录/YYYY-MM-DD-简短描述.md`
2. 更新 `output/index.md`
3. 在 `log/` 当天日志中追加记录

### 4. Lint（维护检查）

检查范围仅限 `wiki/`。具体检查项和执行方式见 [[schema/lint-rules.md]]。

检查完产出体检报告到 `lint/YYYY-MM-DD.md`。问题分两类：可直接修复→确认后执行；需要判断→等用户决定。

### 5. Daily（日常对话）

不属于以上任何一种流程的对话——改配置、查信息、讨论想法、工具调研等——直接响应即可，不强制走任何流程。

对话结束时，如用户说"记一下"，将本次对话中的关键操作以 `daily` 类型记录到 `log/` 当天的日志中。

#### Daily Review（每日复盘）

用户开始每日复盘时，按 [[schema/daily-review-rules.md]] 执行：

1. 先恢复长期上下文并完成事实校正，再保存到 `inbox/dailyreview/YYYY-MM/YYYY-MM-DD.md`。
2. 用户确认“落盘”后，保存复盘并立即进入候选待办阶段；**不得只返回文件路径就结束复盘流程**。
3. 先完整提取用户已经明确表达的课程、交付、跟进和日期承诺；这些硬日程不得只写在复盘正文中，也不受“1—3 项”限制。
4. 再默认提出 1—3 个高价值建议任务，展示任务、完成标准、建议日期、优先级、目标计划和复盘来源。
5. **候选任务不得自动写入日程。** 用户可以修改、删除或调整日期；只有最终版本获得逐项明确确认后，才能写入 `inbox/tasks/` 下 `type: plan`、`status: active` 的计划文件。
6. 用户只说“落盘”代表保存复盘并进入候选建议阶段，不代表授权写入任务；模糊同意也不视为授权。
7. 写入前查重，写入后验证工作台语法，并在复盘与当日日志记录已转化的日程。
8. 只有任务已确认并验证写入、用户明确跳过日程，或明确说明本次无候选任务，完整复盘流程才结束。

## 目录边界速查

- inbox/ 不放：已应用的方法（→ skills/output）
- wiki/ 不放：未消化的素材（→ raw）
- skills/ 不放：项目特定流程（→ output/项目目录）
- output/ 不放：通用方法论（→ skills）


## 领域（Domain）分类

通过 frontmatter 的 `domain` 字段标注页面所属领域（路径式，最多 4 个）。阈值规则、ingest 判断流程、索引维护规范见 [[schema/domain-rules.md]]，当前领域清单（领域目录、子领域清单）和各领域页面分布见 [[wiki/index.md]]。

> **0 页领域：** 矩阵只列 ≥1 页的领域，`wiki/domains/` 分表文件在首个页面出现时才创建，之后即使页面归零也保留。

## 页面格式规范

所有 wiki 和 output 页面必须包含 frontmatter，格式模板详见 [[schema/templates.md]]。

### 正文规范

- 使用 `[[wikilink]]` 进行页面间交叉引用
- 引用原始素材时使用 `[[raw/文件名.md]]`
- 引用 wiki 知识时使用 `[[wiki/xxx/文件名.md]]`
- wiki 页面结构：概述 → 详细内容 → 关联链接
- output 页面结构：需求背景 → 正文内容 → 结论/要点
- 在「关联页面」章节列出所有相关的 wiki 页面链接

## 文件命名规范

- 使用中文或有意义的英文短语
- 使用连字符 `-` 分隔单词（如 `large-language-model.md`）
- source 页面：与 raw 素材同名或简化命名
- entity 页面：使用实体本名（如 `Andrej-Karpathy.md`）
- concept 页面：使用概念名称（如 `RAG检索增强生成.md`）
- inbox 页面：按子目录分类，`news/` 下按日期，`learning/` 下按课程/书名，`sparks/` 下按主题
- skills 页面：使用技能名称（如 `提示词写作.md`、`竞品分析方法.md`）
- output 页面：`output/项目目录/YYYY-MM-DD-简短描述.md`，按项目/目的归入对应子目录。跨项目适用的交付物可放在 `output/其他/`。项目特定的操作流程也放在对应项目目录中。

## 格式模板与日志

页面和日志的格式模板见 [[schema/templates.md]]。

log/ 按月分目录，每天一个文件（`log/YYYY-MM/YYYY-MM-DD.md`），操作类型：`ingest` | `query` | `enrich` | `output` | `lint` | `update` | `daily`。
